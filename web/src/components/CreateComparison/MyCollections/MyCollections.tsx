import CollectionCreate from '../ExcerptLibrary/CollectionCreate/CollectionCreate';
import React from 'react';

type Props = {};

const MyCollections = (props: Props) => {
  return (
    <div>
      <CollectionCreate />
    </div>
  );
};

export default MyCollections;
